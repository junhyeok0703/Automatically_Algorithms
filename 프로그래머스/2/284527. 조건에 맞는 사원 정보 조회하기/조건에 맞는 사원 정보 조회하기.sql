SELECT
    SCORE,
    E.EMP_NO,
    E.EMP_NAME,
    E.POSITION,
    E.EMAIL
FROM (
SELECT 
    EMP_NO,
    SUM(SCORE) as SCORE
FROM HR_GRADE
GROUP BY EMP_NO
ORDER BY SCORE DESC
LIMIT 1) as M
JOIN HR_EMPLOYEES E
ON M.EMP_NO = E.EMP_NO
