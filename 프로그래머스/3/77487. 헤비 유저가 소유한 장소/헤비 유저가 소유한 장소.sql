-- 코드를 입력하세요
WITH T AS(
SELECT 
    HOST_ID
FROM PLACES
GROUP BY HOST_ID
HAVING COUNT(ID)>=2
)

SELECT 
    PLACES.ID,
    PLACES.NAME,
    PLACES.HOST_ID
FROM T
LEFT JOIN PLACES
ON T.HOST_ID = PLACES.HOST_ID
ORDER BY PLACES.ID